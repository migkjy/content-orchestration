# =============================================================================
# AI AppPro Content Pipeline - 환경 변수
# =============================================================================
# .env 파일로 복사하여 값을 입력하세요: cp .env.example .env
# .env 파일은 .gitignore에 의해 버전 관리에서 제외됩니다.
# =============================================================================

# -----------------------------------------------------------------------------
# NeonDB (필수)
# -----------------------------------------------------------------------------
# PostgreSQL 데이터베이스 연결 URL.
# 블로그 포스트, 수집된 뉴스, 생성된 뉴스레터가 저장됩니다.
# 발급: 자비스 VP가 제공 (NeonDB 콘솔: https://console.neon.tech)
DATABASE_URL=postgresql://neondb_owner:...@ep-xxx.aws.neon.tech/neondb?sslmode=require

# -----------------------------------------------------------------------------
# Anthropic Claude API (선택 - 없으면 mock 모드)
# -----------------------------------------------------------------------------
# 뉴스레터 콘텐츠 자동 생성에 사용됩니다.
# 미설정 시: 수집된 뉴스를 기반으로 mock 뉴스레터를 생성합니다.
# 설정 시: Claude Sonnet이 고품질 한국어 뉴스레터를 자동 생성합니다.
# 발급: https://console.anthropic.com > API Keys
# 예상 비용: ~$0.05/뉴스레터 (월 4회 = ~$0.20/월)
ANTHROPIC_API_KEY=sk-ant-api03-...

# -----------------------------------------------------------------------------
# Brevo 이메일 마케팅 (선택 - 없으면 이메일 캠페인 스킵, mock 모드)
# -----------------------------------------------------------------------------
# 뉴스레터 이메일 캠페인 발송 및 구독자 관리에 사용됩니다.
# 미설정 시: 이메일 발송 단계를 스킵합니다.
# 설정 시: 뉴스레터 생성 후 Brevo 리스트에 캠페인을 자동 발송합니다.
# 발급: https://app.brevo.com > Settings > API Keys
# SDK: @getbrevo/brevo
BREVO_API_KEY=xkeysib-...
# Brevo 구독자 리스트 ID (Brevo 대시보드 > Contacts > Lists에서 확인)
BREVO_LIST_ID=0
# 발신자 이름/이메일 (미설정 시 기본값: "AI AppPro" / hello@apppro.kr)
BREVO_SENDER_NAME=AI AppPro
BREVO_SENDER_EMAIL=hello@apppro.kr

# -----------------------------------------------------------------------------
# getlate.dev SNS 배포 API (선택 - 없으면 SNS 배포 스킵)
# -----------------------------------------------------------------------------
# 13개 SNS 플랫폼(Twitter/X, Instagram, LinkedIn, Facebook, TikTok 등)에
# 단일 API로 콘텐츠를 배포하는 서비스.
# 미설정 시: SNS 배포 단계를 스킵합니다.
# 설정 시: 뉴스레터 발행 후 연결된 모든 SNS에 자동 배포합니다.
# 발급: https://getlate.dev > Settings > API Keys
# 계정: migkjy@gmail.com (설정 완료)
# 주의: getlate.dev 대시보드에서 SNS 계정 연결 필요 (현재 미연결 상태)
GETLATE_API_KEY=sk_...

# -----------------------------------------------------------------------------
# Cloudflare R2 (마이그레이션 스크립트용)
# -----------------------------------------------------------------------------
# WP → Next.js 마이그레이션 시 이미지 업로드에 사용됩니다.
# 발급: Cloudflare 대시보드 > R2 > API Tokens
CLOUDFLARE_R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_BUCKET_NAME=business-builder
R2_PUBLIC_URL=https://pub-xxxx.r2.dev

# -----------------------------------------------------------------------------
# DB (마이그레이션 전용 — neondb_owner 권한 필요)
# -----------------------------------------------------------------------------
# migrate-wp.ts 등 스키마 변경이 필요한 마이그레이션 스크립트용.
# 일반 런타임에는 DATABASE_URL (app_user)을 사용합니다.
MIGRATION_DATABASE_URL=postgresql://neondb_owner:...@ep-xxx.aws.neon.tech/neondb?sslmode=require

# -----------------------------------------------------------------------------
# Google Analytics 4 (선택)
# -----------------------------------------------------------------------------
# GA4 페이지뷰 추적에 사용됩니다.
# 미설정 시: GA 스크립트가 로드되지 않습니다.
# 발급: GA4 대시보드 > 관리 > 데이터 스트림 > 측정 ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
